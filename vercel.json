{
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "buildCommand": "corepack enable && pnpm --filter @skillhub/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
